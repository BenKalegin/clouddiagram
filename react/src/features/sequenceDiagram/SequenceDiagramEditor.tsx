import {useAppSelector} from "../../app/hooks";
import {selectDiagramEditor} from "../classDiagram/diagramEditorSlice";

export const SequenceDiagramEditor = () => {
    //const {diagram, selectedElements, focusedElement} = useAppSelector(state => selectDiagramEditor(state));
    return (<div>Sequence here</div>)
}